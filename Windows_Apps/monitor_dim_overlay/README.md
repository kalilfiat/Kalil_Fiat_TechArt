Monitor Dim Overlay

Windows utility that dims secondary monitors while gaming.

Features:
- Adaptive dim overlays
- Mouse reveal on secondary monitors
- Per-monitor behavior
- F9 toggle
- System tray opacity controls

Run:
python monitor_dim_overlay.py

Build:
python -m PyInstaller --onefile --windowed monitor_dim_overlay.py
